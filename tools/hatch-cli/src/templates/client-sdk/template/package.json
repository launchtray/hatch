{
  "name": "@launchtray/hatch-template-client-sdk",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "license": "MIT",
  "private": true,
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "prepare": "rimraf dist && tsc",
    "lint": "eslint --max-warnings=0 .",
    "lint:fix": "eslint --fix .",
    "build": "rimraf dist && npm run lint && tsc"
  },
  "dependencies": {
    "cross-fetch": "^3.0.4"
  },
  "devDependencies": {
    "@types/node": "^12",
    "@launchtray/hatch-client-sdk": "^0.12.5",
    "typescript": "^4.1.3",
    "rimraf": "3.0.0",
    "eslint": "^7.17.0",
    "@launchtray/hatch-eslint-config": "^0.12.5"
  }
}
