import React from 'react';
import {StyleSheet, View} from 'react-native';

interface PropTypes {

}

export default class HATCH_CLI_TEMPLATE_VAR_moduleName extends React.PureComponent<PropTypes> {
  public render() {
    return (
      <View>
      </View>
    );
  }
}

const styles = StyleSheet.create({

});
