import React from 'react';
import {StyleSheet, View} from 'react-native';
import {connect} from 'react-redux';

interface PropTypes {

}

class HATCH_CLI_TEMPLATE_VAR_moduleName extends React.Component<PropTypes> {
  public render() {
    return (
      <View>
      </View>
    );
  }
}

const styles = StyleSheet.create({

});

export default connect(
  (state) => ({

  }),
  (dispatch) => ({

  })
)(HATCH_CLI_TEMPLATE_VAR_moduleName);
